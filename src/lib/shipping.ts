/**
 * Calculate shipping charge based on total quantity of items
 * All shipping is FREE - returns 0 for all orders
 */
export function calculateShippingCharge(totalQuantity: number): number {
  return 0;
}
